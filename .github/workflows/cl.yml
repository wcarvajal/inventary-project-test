name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  DB_CONNECTION: mysql
  DB_HOST: 127.0.0.1
  DB_PORT: 3306
  DB_USERNAME: root
  DB_PASSWORD: ''

jobs:
  products-service:
    name: Products Service Tests
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
          MYSQL_DATABASE: products
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'
        extensions: mbstring, zip, mysql, xml
        coverage: pcov

    - name: Copy .env
      run: |
        cp products-service/.env.example products-service/.env
        sed -i 's/DB_HOST=.*/DB_HOST=127.0.0.1/' products-service/.env
        sed -i 's/DB_DATABASE=.*/DB_DATABASE=products/' products-service/.env

    - name: Install Dependencies
      working-directory: ./products-service
      run: |
        composer install --prefer-dist --no-interaction

    - name: Run Migrations
      working-directory: ./products-service
      run: |
        php artisan migrate --force

    - name: Execute Tests
      working-directory: ./products-service
      run: |
        php artisan test --coverage --min=80

  inventory-service:
    name: Inventory Service Tests
    runs-on: ubuntu-latest
    needs: products-service
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: 'yes'
          MYSQL_DATABASE: inventory
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.2'
        extensions: mbstring, zip, mysql, xml
        coverage: pcov

    - name: Copy .env
      run: |
        cp inventory-service/.env.example inventory-service/.env
        sed -i 's/DB_HOST=.*/DB_HOST=127.0.0.1/' inventory-service/.env
        sed -i 's/DB_DATABASE=.*/DB_DATABASE=inventory/' inventory-service/.env
        sed -i 's/PRODUCTS_SERVICE_URL=.*/PRODUCTS_SERVICE_URL=http:\/\/localhost:8001/' inventory-service/.env

    - name: Install Dependencies
      working-directory: ./inventory-service
      run: |
        composer install --prefer-dist --no-interaction

    - name: Run Migrations
      working-directory: ./inventory-service
      run: |
        php artisan migrate --force

    - name: Execute Tests
      working-directory: ./inventory-service
      run: |
        php artisan test --coverage --min=80